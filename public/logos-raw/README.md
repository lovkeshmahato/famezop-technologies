# Raw client logos (staging)

Drop the original client-logo image files here (any format/size — PNG, JPG,
SVG). This folder is a temporary drop point only: once Claude processes a
logo (resizing, recoloring, optimizing) into its final site-ready asset
under `public/logos/`, the corresponding file here can be deleted.
